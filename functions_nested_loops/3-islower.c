#include "main.h"

/**
 * _islower - checks if a character is lowercase c
 *
 * Return 1 if the c is lowercase
 */

int _islower(int c)
{
	if (_islower (c))
	{
		c =1;
	}
	else
	{
		c = 0;
	}
	return (c);
}
